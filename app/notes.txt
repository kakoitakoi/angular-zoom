
Идея - более качественное извлечение данных из jira и confluence с помощью отдельных агентов. Сейчас данные в промпт попадают не контролируемо - раздувают и размывают контекст, обрезаются по длине теряя смысл. Вместо этого нужно сделать агентов которые будут юзать mcp и отдавать уже обработанный осмысленный  готовый короткий текст  - 10-20 фактов про таблицу с датами, назначение таблицы, 1-5 вопросов на которые отвечает таблица. Это должно забуcтить качество семантического слоя существенно.


Доменная архитектура SQL метрики:
1. При первичном описании таблицы извлекаем селекты в которых участвуют таблицы присутствующие в AI_catalog (нужен маппинг??? или как-то заскриптуем, как скриптовать если данные вне SR???)
2. Нужно ли генерировать sql сущности по исходным таблицам???
3. Нужно генерировать два раза - селекты по имени каталога и селекты по исходному имени???
4. Как удалять метрики сгенерированные по умолчанию из домена????
5. Нужно генерировать sql-сущности в пределах всех таблиц которые есть в AI_catalog. Можем просто брать список или нужно проверять каждую таблицу, от чего это зависит и на что влияет?
6. Нужно сформулировать четкое описание для каждой sql сущности с определением ценности каждой для каждой с точки зрения использования семантического слоя ai-агентами и ai-ассистентами (text2sql чат). Вопросы - от какого типа sql сущности можем смело  отказаться? Какие типы можем объединить? Какие потери или риски есть в случае если оставлю только бизнес сценарии? Мотивация сокращения типов чанков - управление чанками будет передано пользователям( через веб интерфейс) где они смогут изменять, добавлять и удалять чанки. Потенциальная проблема - текущая структура чанков откажется слишком сложной и непонятной для пользователей, текущая архитектура генерации метаданных порождает огромное количество sql сущностей и пользователь не сможет сориентироваться в этом объеме данных описаний (проблема объема усугубляется проблемой текущей структуры)